SMPP 중소기업 / 여성기업 유효기간 조회 기능 설계서
1. 목적

기존 협력업체 엑셀 관리용 파이썬 GUI 프로그램에
smpp.go.kr(중소기업현황 정보시스템)의 데이터를 활용하여

소기업(중소기업) 여부 + 확인일자 + 만료일자

여성기업 여부 + 확인일자 + 만료일자

를 자동으로 조회한 뒤,
협력업체 엑셀에 값을 채워 넣는 기능을 추가한다.

2. 대상 시스템 / 환경

언어: Python 3.x

GUI: 기존 프로그램 (예: PyQt / Tkinter 등, 여기서는 구체 GUI 프레임워크와 무관하게 설계)

엑셀 다루기: pandas 또는 openpyxl (기존 프로그램 상황에 맞게 택1)

HTTP 통신 라이브러리: requests

HTML 파싱: BeautifulSoup4

3. 전체 처리 흐름 (고수준)

사용자 입력

SMPP 로그인용 ID / PW

엑셀 파일 경로

사용할 시트명

사업자등록번호가 들어있는 열(컬럼) 위치

결과(소기업/여성기업 유효기간)를 쓸 열(컬럼) 위치

SMPP 로그인

POST https://www.smpp.go.kr/uat/uia/actionLogin.do

Form Data: id, password

응답의 Location 헤더에 loginSuccess 포함 여부로 성공/실패 판단

requests.Session() 으로 세션 쿠키(JSESSIONID) 유지

업체별 반복 처리

엑셀에서 사업자등록번호 목록 읽기

각 사업자번호(biz_no)에 대해:

사업자번호로 업체 목록 조회

POST https://www.smpp.go.kr/cop/registcorp/selectRegistCorpListVw.do

Form Data 중 searchBsnmNo 에 사업자번호 입력

응답 HTML(업체 목록)에서 선택 대상 업체의 key(corp_key) 추출
(예상: registCorpSn 같은 값)

상세 요약(기업특징) 페이지 요청

POST https://www.smpp.go.kr/cop/registcorp/selectRegistCorpSumryInfoVw.do

Form Data 에 corp_key 포함

응답 HTML에서 “기업특징” 표를 파싱

기업특징 표 파싱

<tbody> <tr> 반복

구분 컬럼이 "소기업" 인 행 → 소기업 확인일자 / 만료일자

구분 컬럼이 "여성기업" 인 행 → 여성기업 확인일자 / 만료일자

없거나 “해당사항 없음”이면 None/빈 값 처리

결과를 메모리에 리스트/딕셔너리 형태로 수집

엑셀 반영

기존 엑셀에 결과 컬럼을 추가/갱신

예:

소기업_확인일자

소기업_만료일자

여성기업_확인일자

여성기업_만료일자

GUI 반영

“SMPP 중소·여성기업 유효기간 조회” 버튼/메뉴 추가

실행 중 진행률 표시 (예: ProgressBar, 상태 텍스트)

실패한 업체에 대한 에러 메시지/로그 표시

4. HTTP 요청 사양 (상세)
4.1 로그인 요청

URL

POST https://www.smpp.go.kr/uat/uia/actionLogin.do


Form Data

id:        <사용자 ID>
password:  <사용자 PW>


헤더(예시)

headers = {
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) "
                  "AppleWebKit/537.36 (KHTML, like Gecko) "
                  "Chrome/142.0.0.0 Safari/537.36",
    "Origin": "https://www.smpp.go.kr",
    "Referer": "https://www.smpp.go.kr/uat/uia/actionLogin.do",
}


성공 조건

allow_redirects=False 로 POST 요청

응답 status code 가 301/302 이고

resp.headers["Location"] 안에 "loginSuccess" 문자열 포함

실패 시 처리

함수에서 예외 발생 또는 False 반환

GUI 쪽에서 “로그인 실패” 팝업 표시

4.2 사업자등록번호로 업체 목록 조회

URL

POST https://www.smpp.go.kr/cop/registcorp/selectRegistCorpListVw.do


Form Data (핵심)
아래는 실제 Request Payload를 정리한 형태 (값은 예시):

data = {
    "chks": "",
    "fileType": "",
    "pageIndex": "1",
    "ctprvnNm": "전체",
    "signguNm": "전체",
    "cntrctEsntlNo": "",
    "entrpsNm": "",
    "searchBsnmNo": biz_no,  # ★ 여기만 업체마다 바뀜
    "chargerNm": "",
    "detailPrdnm": "",
    "detailPrdnmNo": "",
    "ksicNm": "",
    "ksic": "",
    "prductNm": "",
    "ctprvnCode": "",
    "signguCode": "",
    "smbizCode": "",
    "femtrbleCode": "",
    "hitechCode": "",
    "envqualCode": "",
    "entrpsNmMbl": "",
    "searchBsnmNoMbl": "",
    "chargerNmMbl": "",
    "pageUnit": "15",
    # "header_username": "<로그인 ID>"  # 필요 시 포함 (테스트 필요)
}


응답

HTML 문서 (업체 목록 화면)

이 HTML 안에서 첫 번째 업체의 “상세/요약”으로 들어가는 key를 추출해야 함

예: <a href="...selectRegistCorpSumryInfoVw.do?registCorpSn=12345"> 형태

또는 <input type="hidden" name="registCorpSn" value="12345"> 등

설계상 추상화

목록 HTML에서 대표 업체의 key를 추출하는 함수:

def extract_corp_key_from_list_html(html: str) -> str | None:
    """
    업체 목록 HTML에서 상세 조회에 사용할 corp_key (예: registCorpSn)를 추출.
    첫 번째 행 기준으로 추출. 찾을 수 없으면 None 반환.
    """


실제 구현 시 Codex가 목록 HTML 구조를 한 번 더 확인해서,

href 속성 또는

hidden input 값

JavaScript function 호출 인자
중에서 적절한 값을 파싱하도록 구현.

4.3 상세 요약(기업특징) 페이지 요청

URL

POST https://www.smpp.go.kr/cop/registcorp/selectRegistCorpSumryInfoVw.do


Form Data (예상)

목록 HTML에서 추출한 corp_key(예: registCorpSn) 를 포함

실제 Form Data 이름은 개발자도구 Network → Payload 에서 확인 필요

설계상 추상화:

data = {
    "registCorpSn": corp_key,  # 또는 실제 필드명에 맞춰 수정
    # 나머지 필드가 있다면 그대로 전달
}


응답

HTML 문서

“기업특징” 표가 포함된 상세 요약 화면

기업특징 표 구조 (요약)

<div class="tabContent a2">
  <span class="labelType1">기업특징</span>
  <div class="tbl_type col">
    <table ...>
      <thead>
        <tr>
          <th>구분</th>
          <th>확인기관</th>
          <th>확인일자</th>
          <th>만료일자</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>소기업</td>
          <td>중소벤처기업부</td>
          <td>2025-04-01</td>
          <td>2026-03-31</td>
        </tr>
        <tr>
          <td>여성기업</td>
          <td>...</td>
          <td>2024-08-14</td>
          <td>2027-08-13</td>
        </tr>
        <tr>
          <td>장애인기업</td>
          <td colspan="3">해당사항 없음</td>
        </tr>
        ...
      </tbody>
    </table>
  </div>
</div>

5. 파이썬 모듈 구조 설계
5.1 smpp_client.py – SMPP HTTP 클라이언트

SMPP 사이트와 통신만 담당하는 모듈.

# smpp_client.py

from dataclasses import dataclass
import requests


LOGIN_URL = "https://www.smpp.go.kr/uat/uia/actionLogin.do"
LIST_URL = "https://www.smpp.go.kr/cop/registcorp/selectRegistCorpListVw.do"
SUMMARY_URL = "https://www.smpp.go.kr/cop/registcorp/selectRegistCorpSumryInfoVw.do"


@dataclass
class SmppCredentials:
    user_id: str
    password: str


class SmppClient:
    def __init__(self, creds: SmppCredentials):
        self.creds = creds
        self.session = requests.Session()

    def login(self) -> None:
        """SMPP 로그인. 실패 시 예외 발생."""
        headers = {
            "User-Agent": "...크롬 UA...",
            "Origin": "https://www.smpp.go.kr",
            "Referer": LOGIN_URL,
        }
        data = {
            "id": self.creds.user_id,
            "password": self.creds.password,
        }
        resp = self.session.post(
            LOGIN_URL,
            headers=headers,
            data=data,
            allow_redirects=False,
        )
        location = resp.headers.get("Location", "")
        if resp.status_code not in (301, 302) or "loginSuccess" not in location:
            raise RuntimeError("SMPP 로그인 실패")

    def fetch_list_html_by_biz_no(self, biz_no: str) -> str:
        """사업자등록번호로 업체 목록 HTML 조회."""
        data = {
            "chks": "",
            "fileType": "",
            "pageIndex": "1",
            "ctprvnNm": "전체",
            "signguNm": "전체",
            "cntrctEsntlNo": "",
            "entrpsNm": "",
            "searchBsnmNo": biz_no,
            "chargerNm": "",
            "detailPrdnm": "",
            "detailPrdnmNo": "",
            "ksicNm": "",
            "ksic": "",
            "prductNm": "",
            "ctprvnCode": "",
            "signguCode": "",
            "smbizCode": "",
            "femtrbleCode": "",
            "hitechCode": "",
            "envqualCode": "",
            "entrpsNmMbl": "",
            "searchBsnmNoMbl": "",
            "chargerNmMbl": "",
            "pageUnit": "15",
            # "header_username": self.creds.user_id,  # 필요 시 테스트 후 활성화
        }
        resp = self.session.post(LIST_URL, data=data)
        resp.raise_for_status()
        return resp.text

    def fetch_summary_html(self, corp_key: str) -> str:
        """
        업체별 상세 요약(기업특징 포함) HTML 조회.
        corp_key는 목록 HTML에서 추출한 값 (예: registCorpSn).
        """
        data = {
            "registCorpSn": corp_key,  # 실제 필드명은 DevTools로 확인 후 수정
        }
        resp = self.session.post(SUMMARY_URL, data=data)
        resp.raise_for_status()
        return resp.text


⚠ registCorpSn 이름은 예상 값이므로,
실제 Form Data 필드명을 한번 DevTools에서 확인해서 맞춰야 한다 (Codex TODO).

5.2 smpp_parser.py – HTML 파싱
# smpp_parser.py

from dataclasses import dataclass
from typing import Optional
from bs4 import BeautifulSoup


@dataclass
class CorpFeatures:
    small_exists: bool
    small_confirm_date: Optional[str]
    small_expire_date: Optional[str]
    women_exists: bool
    women_confirm_date: Optional[str]
    women_expire_date: Optional[str]


def extract_corp_key_from_list_html(html: str) -> Optional[str]:
    """
    업체 목록 HTML에서 상세 조회에 사용할 corp_key 추출.
    - 첫 번째 업체 기준
    - 구현 시:
      - <a href="...selectRegistCorpSumryInfoVw.do?registCorpSn=12345"> 형태를 찾거나
      - JavaScript 함수 인자, hidden input 등에서 추출
    """
    soup = BeautifulSoup(html, "html.parser")

    # TODO: Codex가 실제 HTML 구조를 보고 구현
    # 예시 (href에 registCorpSn=12345 형태가 있다고 가정):
    # link = soup.find("a", href=lambda h: h and "selectRegistCorpSumryInfoVw.do" in h)
    # if not link:
    #     return None
    # from urllib.parse import urlparse, parse_qs
    # qs = parse_qs(urlparse(link["href"]).query)
    # return qs.get("registCorpSn", [None])[0]

    return None  # 구현 필요


def parse_corp_features(html: str) -> CorpFeatures:
    """
    상세 요약 HTML에서 '기업특징' 표를 찾아
    소기업 / 여성기업의 확인일자 및 만료일자를 추출한다.
    """
    soup = BeautifulSoup(html, "html.parser")

    # 기업특징 탭 영역 찾기
    tab = soup.find("div", class_="tabContent a2")
    if not tab:
        # 구조 변경 등 예외 상황
        return CorpFeatures(False, None, None, False, None, None)

    table = tab.find("table")
    if not table:
        return CorpFeatures(False, None, None, False, None, None)

    small_exists = False
    small_c = small_e = None
    women_exists = False
    women_c = women_e = None

    for tr in table.find("tbody").find_all("tr"):
        tds = tr.find_all("td")
        if not tds:
            continue

        kind = tds[0].get_text(strip=True)  # "소기업", "여성기업", ...

        # '해당사항 없음' 처리
        if len(tds) == 2 and "해당사항 없음" in tds[1].get_text():
            continue
        if len(tds) == 4 and "해당사항 없음" in tds[1].get_text():
            continue

        # 소기업
        if kind == "소기업" and len(tds) >= 4:
            small_exists = True
            small_c = tds[2].get_text(strip=True) or None
            small_e = tds[3].get_text(strip=True) or None

        # 여성기업
        if kind == "여성기업" and len(tds) >= 4:
            women_exists = True
            women_c = tds[2].get_text(strip=True) or None
            women_e = tds[3].get_text(strip=True) or None

    return CorpFeatures(
        small_exists=small_exists,
        small_confirm_date=small_c,
        small_expire_date=small_e,
        women_exists=women_exists,
        women_confirm_date=women_c,
        women_expire_date=women_e,
    )

5.3 smpp_service.py – 엑셀/GUI와 연결하는 서비스
# smpp_service.py

from dataclasses import dataclass
from typing import List, Optional

from smpp_client import SmppClient, SmppCredentials
from smpp_parser import extract_corp_key_from_list_html, parse_corp_features, CorpFeatures


@dataclass
class CorpCheckResult:
    biz_no: str
    corp_key: Optional[str]
    features: Optional[CorpFeatures]
    error: Optional[str] = None


def check_corps(
    user_id: str,
    password: str,
    biz_nos: List[str],
    progress_callback=None,
) -> List[CorpCheckResult]:
    """
    여러 사업자번호에 대해 SMPP에서 소기업/여성기업 정보를 조회한다.
    progress_callback: 진행상황을 GUI에 반영하기 위한 콜백 (optional)
    """
    creds = SmppCredentials(user_id=user_id, password=password)
    client = SmppClient(creds)
    client.login()

    results: List[CorpCheckResult] = []

    total = len(biz_nos)
    for idx, biz_no in enumerate(biz_nos, start=1):
        if progress_callback:
            progress_callback(idx, total, biz_no)

        try:
            list_html = client.fetch_list_html_by_biz_no(biz_no)
            corp_key = extract_corp_key_from_list_html(list_html)
            if not corp_key:
                results.append(CorpCheckResult(biz_no, None, None, "검색 결과 없음"))
                continue

            summary_html = client.fetch_summary_html(corp_key)
            features = parse_corp_features(summary_html)
            results.append(CorpCheckResult(biz_no, corp_key, features, None))
        except Exception as e:
            results.append(CorpCheckResult(biz_no, None, None, str(e)))

    return results

6. 엑셀 및 GUI 연동 설계
6.1 엑셀 구조 가정

예시 가정 (실제 구조에 맞게 변경):

A열: 업체명

B열: 사업자등록번호 (하이픈 포함/미포함 상관 없음, 필요 시 정규화)

결과 칼럼 (신규 추가):

소기업_확인일자

소기업_만료일자

여성기업_확인일자

여성기업_만료일자

6.2 엑셀 업데이트 흐름 (pandas 예시 설계)
import pandas as pd
from smpp_service import check_corps

def update_excel_with_smpp_info(
    excel_path: str,
    sheet_name: str,
    id: str,
    password: str,
    biz_no_column: str,
):
    df = pd.read_excel(excel_path, sheet_name=sheet_name)

    biz_nos = df[biz_no_column].astype(str).tolist()

    # Codex 구현 시 GUI progress_callback을 넘길 수도 있음.
    results = check_corps(id, password, biz_nos)

    # 결과를 DataFrame에 매핑
    # biz_no 를 key로 dict 만든다 (같은 번호 중복 없다는 가정)
    result_map = {r.biz_no: r for r in results}

    small_c_list = []
    small_e_list = []
    women_c_list = []
    women_e_list = []

    for biz_no in biz_nos:
        r = result_map.get(str(biz_no))
        if not r or not r.features:
            small_c_list.append(None)
            small_e_list.append(None)
            women_c_list.append(None)
            women_e_list.append(None)
        else:
            f = r.features
            small_c_list.append(f.small_confirm_date)
            small_e_list.append(f.small_expire_date)
            women_c_list.append(f.women_confirm_date)
            women_e_list.append(f.women_expire_date)

    df["소기업_확인일자"] = small_c_list
    df["소기업_만료일자"] = small_e_list
    df["여성기업_확인일자"] = women_c_list
    df["여성기업_만료일자"] = women_e_list

    # 기존 파일 덮어쓰기 or 다른 이름으로 저장
    df.to_excel(excel_path, sheet_name=sheet_name, index=False)

6.3 GUI 연동 포인트

GUI 쪽에서는 대략 이런 흐름:

설정 입력 UI

SMPP ID/PW 입력란

엑셀 파일 선택 (파일 다이얼로그)

시트 선택 드롭다운

사업자번호 컬럼 선택

실행 버튼 클릭 시

위 설정값들을 읽어와서 update_excel_with_smpp_info(...) 호출

별도 스레드/Worker로 실행하여 GUI 프리징 방지

progress_callback 을 이용해 ProgressBar 업데이트

완료 후

“완료되었습니다” 메시지,

에러/조회 실패 건수가 있으면 간단한 로그 팝업 표시

7. 예외 처리 / 고려 사항

로그인 실패

ID/PW 오류, 사이트 변경 등

SmppClient.login()에서 예외 발생 → GUI에서 팝업 처리

검색 결과 없음

해당 사업자번호에 대한 등록기업이 없을 때

extract_corp_key_from_list_html 가 None 반환

CorpCheckResult.error = "검색 결과 없음"

기업특징 표 없음

구조 변경 / 탭 비활성 등

parse_corp_features 가 False, None, None, False, None, None 반환

엑셀에는 빈 값으로 표시

속도

업체 수가 많을 경우 사이트에 부담을 줄이기 위해

time.sleep(0.3~1.0초) 정도 지연을 넣는 것도 고려

추후 멀티스레드/멀티프로세스 병렬 처리도 가능하지만,

사이트 부하/차단 위험 때문에 초기 버전은 직렬 처리 권장

사업자번호 포맷

엑셀에 216-81-15499, 2168115499 등 여러 포맷으로 있을 수 있음

SMPP에 어떤 포맷을 넣어도 조회가 되는지 확인 필요

필요 시 정규화 함수 추가:

def normalize_biz_no(raw: str) -> str:
    digits = re.sub(r"\D", "", raw)
    # 필요하다면 XXX-XX-XXXXX 형태로 다시 포맷팅
    return digits  # 우선 숫자만 사용하는 방식 등

8. Codex 구현 시 체크리스트

로그인 함수 구현

SmppClient.login() 정상 동작 확인

잘못된 ID/PW일 때 예외/로그 확인

사업자번호 검색

fetch_list_html_by_biz_no 호출 후 HTML 저장해서 로컬에 한 번 확인

extract_corp_key_from_list_html 구현 시
실제 HTML 구조(링크, hidden input 등)를 기준으로 corp_key 파싱

상세 요약 요청

DevTools로 selectRegistCorpSumryInfoVw.do 의 Form Data 필드명 확인

fetch_summary_html 의 data 딕셔너리에서 필드명 수정

기업특징 파싱

parse_corp_features 테스트용 HTML(사용자가 제공한 예시)로 동작 확인

소기업/여성기업이 “해당사항 없음”인 케이스도 추가 테스트

엑셀 연동

실제 협력업체 엑셀 파일 구조에 맞춰

사업자번호 컬럼명

결과 컬럼명
수정

파일 읽기/쓰기 테스트

GUI 연결

기존 GUI 코드에서

ID/PW 입력

파일/시트 선택

실행 버튼/진행률 표시

완료/에러 메시지
연동