# SMPP 상세페이지(기업특징) 조회 실패 원인 & 해결 방법 정리

대상 시스템: https://www.smpp.go.kr (중소기업현황 정보시스템)  
목표: **사업자번호로 업체 검색 → 상세 페이지(기업특징)에서 ‘여성기업’ 확인일자 / 만료일자 크롤링**

---

## 1. 전체 흐름 요약

파이썬 스크립트에서 하는 일은 크게 3단계입니다.

1. **로그인**
2. **사업자등록번호로 업체 목록 조회**
3. **목록에서 특정 업체 상세(기업특징) 페이지로 이동 후 파싱**

이번 이슈는 3번, **상세 페이지로 이동하는 부분**에서 발생한 문제를 디버깅해서 해결한 내용입니다.

---

## 2. 초기에 실패했던 접근 방식

### 2.1. 처음 가정했던 HTML 구조

처음에는 “목록 페이지에 상세 페이지로 가는 링크가 직접 있을 것”이라고 가정하고,  
아래처럼 `href` 에 `selectRegistCorpSumryInfoVw.do` 가 들어간 링크를 찾으려고 했습니다.

```python
link = soup.find("a", href=lambda h: h and "selectRegistCorpSumryInfoVw.do" in h)
그러나 실제 list_debug.html을 열어보니 구조가 전혀 달랐습니다.

2.2. 실제 목록 페이지 구조
검색 결과 테이블에서 업체명 링크는 다음과 같이 되어 있습니다.

html
코드 복사
<a href="#" class="subject" onClick="javascript:fn_moveDetail('2168115499')">
    (주)대상전력
</a>
그리고 페이지 하단에는 다음과 같은 JS 함수와 숨은 폼이 있습니다.

javascript
코드 복사
// 상세화면으로 이동
function fn_moveDetail(bsnmNo){
    var obj = document.moveForm;
    obj.bsnmNo.value = bsnmNo;
    obj.action = "/cop/registcorp/selectRegistCorpSumryInfoVw.do";
    obj.submit();
}
html
코드 복사
<form name="moveForm" method="post">
    <input type="hidden" name="cntrctEsntlNo" value=""/>
    <input type="hidden" name="menuId" value="5040101"/>
    <input type="hidden" name="pageIndex" value="1"/>
    <input type="hidden" name="searchBsnmNo" value="2168115499" title="검색 사업자번호"/>
    <input type="hidden" name="bsnmNo" value="" title="사업자번호"/>
    ...
</form>
즉, 상세 페이지로 가는 “직접적인 링크(href)”가 없고,
클릭 시 fn_moveDetail(bsnmNo) 자바스크립트 함수가 폼(moveForm)에 값을 채워서 POST로 넘기는 구조였습니다.

그래서 href에서 selectRegistCorpSumryInfoVw.do 를 찾는 방식은 항상 실패할 수밖에 없었습니다.

실제로 스크립트에서 “상세 페이지 링크를 찾지 못했습니다.” 라는 메시지가 찍힌 이유가 이것입니다.

3. 해결 전략: 자바스크립트 대신 폼을 직접 흉내 내기
브라우저에서 하는 일을 정리하면:

사용자가 업체명을 클릭 → fn_moveDetail(bsnmNo) 호출

moveForm.bsnmNo = bsnmNo 로 세팅

moveForm.action = "/cop/registcorp/selectRegistCorpSumryInfoVw.do"

moveForm.submit() → POST 요청 발생

이를 파이썬에서 그대로 “흉내 내는” 방식으로 해결했습니다.

3.1. 목록 HTML에서 moveForm 읽기
기존의 “상세 링크 찾기” 대신, 숨은 폼 moveForm을 파싱해서 hidden 필드들을 모두 dictionary로 가져옵니다.

python
코드 복사
import re
from bs4 import BeautifulSoup

def build_move_form_payload_from_list_html(html: str, biz_no: str) -> dict:
    """
    목록 페이지에서 moveForm hidden 필드들을 파싱해서
    상세 요청에 쓸 payload dict 생성.
    - JS fn_moveDetail(bsnmNo)가 하는 일을 그대로 흉내냄:
      -> moveForm.bsnmNo = bsnmNo
      -> action = /cop/registcorp/selectRegistCorpSumryInfoVw.do
    """
    soup = BeautifulSoup(html, "html.parser")

    form = soup.find("form", attrs={"name": "moveForm"})
    if not form:
        raise RuntimeError("moveForm 폼을 찾지 못했습니다.")

    payload = {}
    for inp in form.find_all("input"):
        name = inp.get("name")
        if not name:
            continue
        value = inp.get("value", "")
        payload[name] = value

    # 사업자번호에서 숫자만 추출해서 bsnmNo 에 세팅
    digits = re.sub(r"\D", "", biz_no)
    payload["bsnmNo"] = digits

    # searchBsnmNo 가 비어있으면 같이 채워주기(안 비어있으면 그냥 둠)
    if "searchBsnmNo" in payload and not payload["searchBsnmNo"]:
        payload["searchBsnmNo"] = digits

    return payload
포인트:

form[name="moveForm"] 안의 모든 <input type="hidden">을 그대로 가져와 기본 payload로 사용.

실제 상세 요청에서 바뀌는 값은 bsnmNo 하나이므로,

GUI에서 사용한 사업자번호(BIZ_NO)에서 숫자만 추출해서 payload["bsnmNo"]에 넣습니다.

searchBsnmNo 는 이미 폼에 들어있는 경우가 많지만, 비어 있으면 같이 채워주는 정도만 처리.

3.2. 상세 페이지 요청
위에서 만든 payload를 가지고 selectRegistCorpSumryInfoVw.do 에 POST 요청을 보냅니다.

python
코드 복사
SUMMARY_URL = "https://www.smpp.go.kr/cop/registcorp/selectRegistCorpSumryInfoVw.do"

def fetch_summary_html(session: requests.Session, payload: dict) -> str:
    """상세 요약(기업특징) HTML 조회."""
    resp = session.post(SUMMARY_URL, data=payload)
    print(f"[+] 상세 페이지 응답: {resp.status_code}, url={resp.url}")
    resp.raise_for_status()

    # 디버그용 저장 (필요시)
    with open("summary_debug.html", "w", encoding="utf-8") as f:
        f.write(resp.text)

    return resp.text
이렇게 하면 브라우저에서 fn_moveDetail() → moveForm.submit() 으로 이루어지는 상세 이동 로직을 requests로 그대로 재현하게 됩니다.

4. 기업특징 테이블에서 여성기업 유효기간 파싱
상세 페이지 HTML에는 다음과 같은 “기업특징” 테이블이 있습니다.

html
코드 복사
<span class="labelType1">기업특징</span>
<div class="tbl_type col">
  <table>
    <thead>...</thead>
    <tbody>
      <tr>
        <td>소기업</td>
        <td>중소벤처기업부</td>
        <td>2025-04-01</td>  <!-- 확인일자 -->
        <td>2026-03-31</td>  <!-- 만료일자 -->
      </tr>
      <tr>
        <td>여성기업</td>
        <td>...</td>
        <td>2024-08-14</td>  <!-- 확인일자 -->
        <td>2027-08-13</td>  <!-- 만료일자 -->
      </tr>
      ...
    </tbody>
  </table>
</div>
이를 기준으로 여성기업 행만 골라서 확인일자/만료일자를 뽑는 함수는 다음과 같이 구현했습니다.

python
코드 복사
from bs4 import BeautifulSoup

def parse_women_feature(summary_html: str):
    """
    상세 요약 HTML에서 '여성기업' 행의 확인일자 / 만료일자를 추출.
    없거나 '해당사항 없음'이면 (None, None) 반환.
    """
    soup = BeautifulSoup(summary_html, "html.parser")

    # '기업특징' 라벨 span 기준으로 table 찾기 (class 조합에 덜 민감하게)
    span = soup.find("span", class_="labelType1", string=lambda t: t and "기업특징" in t)
    if not span:
        return None, None

    table = span.find_next("table")
    if not table:
        return None, None

    tbody = table.find("tbody") or table

    for tr in tbody.find_all("tr"):
        tds = tr.find_all("td")
        if not tds:
            continue

        kind = tds[0].get_text(strip=True)
        if kind != "여성기업":
            continue

        # "해당사항 없음" 케이스
        if any("해당사항 없음" in td.get_text() for td in tds[1:]):
            return None, None

        confirm = tds[2].get_text(strip=True) if len(tds) > 2 else None
        expire  = tds[3].get_text(strip=True) if len(tds) > 3 else None

        confirm = confirm or None
        expire  = expire or None

        return confirm, expire

    return None, None
5. 최종 main 흐름 요약
최종 테스트 스크립트의 핵심 흐름은 다음과 같습니다.

python
코드 복사
def main():
    session = requests.Session()

    # 1) 로그인 (loginForm 파싱 → actionLogin.do)
    login(session, USER_ID, USER_PW)

    # 2) 사업자번호로 목록 조회 (selectRegistCorpListVw.do)
    list_html = fetch_list_html(session, BIZ_NO)

    # 3) moveForm 기반 payload 구성 (fn_moveDetail(bsnmNo) 동작 재현)
    payload = build_move_form_payload_from_list_html(list_html, BIZ_NO)

    # 4) 상세 페이지(기업특징) 조회
    summary_html = fetch_summary_html(session, payload)

    # 5) 여성기업 확인일자 / 만료일자 파싱
    women_confirm, women_expire = parse_women_feature(summary_html)

    print("===== 여성기업 정보 =====")
    print("확인일자 :", women_confirm)
    print("만료일자 :", women_expire)
이렇게 수정한 뒤에는:

로그인 성공

목록 조회 성공

상세 페이지 진입 성공

여성기업 확인일자 / 만료일자 정상 파싱

까지 한 번에 동작하는 것을 확인했습니다.

6. Codex 작업 시 참고 사항
로그인 부분

실제로는 egovLoginUsr.do에서 loginForm 전체를 파싱해서 hidden 값까지 포함해 actionLogin.do로 POST하는 방식이라,
폼 전체 직렬화 방식으로 구현하는 게 가장 안정적입니다.

상세 이동

selectRegistCorpSumryInfoVw.do 로 직접 GET/POST하는 것이 아니라,
반드시 moveForm + bsnmNo 방식을 그대로 따라가야 합니다.

사업자번호 포맷

폼 내부에서도 searchBsnmNo는 하이픈 없이 2168115499 형태로 쓰고 있으므로,
코드에서도 re.sub(r"\D", "", biz_no) 로 숫자만 추출해서 쓰는 것이 안전합니다.

HTML 구조 변경 가능성

moveForm의 필드 구성이나 fn_moveDetail 시그니처가 바뀔 수 있으므로,
가능하면 HTML 구조가 조금 바뀌어도 덜 깨지도록

form[name="moveForm"]

input type="hidden"
기반으로 유연하게 파싱하도록 구현하는 것이 좋습니다.

이 문서와 함께 실제 smpp_women_test.py 스크립트를 보면,
상세 페이지 접속 실패 → JS/폼 구조 파악 → moveForm 기반 payload로 해결
까지의 흐름이 한 번에 이해될 거예요.